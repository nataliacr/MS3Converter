import { SchemaObjects } from './../../oas/oas-20-api-interface';

export default function parseSchemaObjects(data: object): SchemaObjects {
  return {
    'name': {
      'title': 'hello'
    }
  };
}